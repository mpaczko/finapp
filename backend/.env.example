DATABASE_URL=""
PORT=4000
CORS_ORIGIN="http://localhost:5173"
SUPABASE_URL=""
SUPABASE_JWKS_URL=""

# Keep true in real environments. For local API exploration you can set
# AUTH_ENABLED=false and send x-user-id, or set LOCAL_USER_ID.
AUTH_ENABLED=true

# Only needed for legacy HS256 projects that do not expose asymmetric JWKS keys.
SUPABASE_JWT_SECRET=""
